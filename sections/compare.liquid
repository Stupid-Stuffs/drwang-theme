{%- style -%}
.compare {
	display: flex;
	align-items: center;
	justify-content: center;
.compare-title {
	font-size: 2.5rem;
	font-weight: 700;
	color: #233a96;
	text-transform: uppercase;
}
.compare-image {
    img {
	width: 100%;
	margin: 0 auto;
	}
}
}

@media (min-width: 750px) {
	.compare {
		.compare-title {
			font-size: 4rem;
		}
	}
}
@media (min-width: 1250px) {
	.compare {
		.compare-title {
			font-size: 5rem;
		}
	}
}
{%- endstyle -%}

<article class="page-width compare">
	<h2 class="compare-title">{{section.settings.title}}</h2>
	<div class="compare-image">
		{% if section.settings.image %}
			<img src="{{ section.settings.image | img_url: 'master' }}" alt="{{ section.settings.alt }}">
		{% endif %}
	</div>
</article>

{%- schema -%}
{
  "name": "compare",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Title"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "alt",
      "label": "Alt",
      "default": "Alt"
    }
  ]
}
{%- endschema -%}
